[package]
name = "rootpass"
version = "0.1.0"
description = "RootPass — Merkle root store on Solana"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "rootpass"

[features]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
cpi = ["no-entrypoint"]
default = []

[dependencies]
anchor-lang = "0.30.1"
